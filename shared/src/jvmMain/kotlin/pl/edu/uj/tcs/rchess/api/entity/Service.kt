package pl.edu.uj.tcs.rchess.api.entity

/**
 * An enum that lists the various services supported by Random Chess.
 */
enum class Service {
    UNKNOWN,
    RANDOM_CHESS,
    LICHESS;

    companion object
}
